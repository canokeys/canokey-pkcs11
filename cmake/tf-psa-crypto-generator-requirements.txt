# Runtime dependencies for TF-PSA-Crypto's generated-file scripts used by
# this project. Keep this narrower than external/tf-psa-crypto/scripts/
# basic.requirements.txt: the upstream file also installs typing stubs and
# pins older MarkupSafe versions for broad development compatibility, neither
# of which is needed for CI builds here.
Jinja2>=3.1
jsonschema>=4.0
