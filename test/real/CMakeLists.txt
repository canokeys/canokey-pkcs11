add_executable(test_abi test_abi.c)
if(APPLE OR UNIX)
  target_link_libraries(test_abi PRIVATE ${CMAKE_DL_LIBS})
endif()
if(RUN_REAL_TESTING)
  add_test(NAME test_abi COMMAND test_abi $<TARGET_FILE:${CNK_PKCS_TARGET}>)
endif()
